spring:
  datasource:
    url: jdbc:mysql://localhost:3306/developer_assessments?useSSL=false&characterEncoding=utf8&serverTimezone=UTC
    username: your_username
    password: your_password
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.developerassessmentsmaster.domain.entity

redis:
  host: localhost
  port: 6379

github:
  api:
    token: your_token
